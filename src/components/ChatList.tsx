// ChatList.tsx
interface Props {
    selectedChat: string;
    onChatSelect: (chatId: string) => void;
  }
  
  const ChatList: React.FC<Props> = ({  }) => {
    return null;
  };
  
  export default ChatList;
  
